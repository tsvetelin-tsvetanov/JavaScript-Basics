function roundNumber(num) {
    var floor = Math.floor(num);
    var round = Math.round(num);
    console.log(floor);
    console.log(round);
}

roundNumber(22.7);
roundNumber(12.3);
roundNumber(58.7);